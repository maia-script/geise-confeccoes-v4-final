import { api } from '../core/api.js';
import { $, $$, esc, pageHero, setSeo } from '../core/utils.js';
import { productCard } from '../components/product-card.js';
import { navigate } from '../core/router.js';
import { icon, hydrateIcons } from '../core/icons.js';
export async function catalogPage({category='',sale=false,isNew=false}={}){const params=new URLSearchParams(location.search);if(category)params.set('category',category);if(sale)params.set('sale','1');if(isNew)params.set('new','1');params.set('limit','24');const [res,cats]=await Promise.all([api(`/api/catalog/products?${params}`),api('/api/catalog/categories')]);const title=category?cats.items.find(c=>c.slug===category)?.name||'Categoria':sale?'Promoções':isNew?'Novidades':'Catálogo completo';setSeo({title,description:`${title} na Geise Confecções. Filtre por categoria, tamanho, cor e preço.`});return `${pageHero(title,res.pagination.total?`${res.pagination.total} produtos encontrados.`:'Explore o catálogo Geise.')}
<section class="section-sm"><div class="container catalog-layout"><aside class="filters" id="filtersDrawer"><form class="filter-panel" id="catalogFilters"><div class="section-head"><h3>Filtros</h3><button type="button" class="icon-btn mobile-only" id="closeFilters">${icon('x')}</button></div><div class="filter-group"><label class="title">Buscar</label><input class="input" name="q" value="${esc(params.get('q')||'')}" placeholder="Ex.: #147 ou vestido preto"></div><div class="filter-group"><label class="title">Categoria</label><select class="select" name="category"><option value="">Todas</option>${cats.items.map(c=>`<option value="${c.slug}" ${params.get('category')===c.slug?'selected':''}>${esc(c.name)}</option>`).join('')}</select></div><div class="filter-group"><label class="title">Preço</label><div style="display:grid;grid-template-columns:1fr 1fr;gap:8px"><input class="input" type="number" min="0" name="min" placeholder="Mín." value="${esc(params.get('min')||'')}"><input class="input" type="number" min="0" name="max" placeholder="Máx." value="${esc(params.get('max')||'')}"></div></div><div class="filter-group"><label class="title">Tamanho</label><input class="input" name="size" value="${esc(params.get('size')||'')}" placeholder="P, M, G, 38..."></div><div class="filter-group"><label class="title">Cor</label><input class="input" name="color" value="${esc(params.get('color')||'')}" placeholder="Preto, azul..."></div><div class="filter-group check-list"><label><input type="checkbox" name="sale" value="1" ${params.get('sale')==='1'?'checked':''}> Em promoção</label><label><input type="checkbox" name="new" value="1" ${params.get('new')==='1'?'checked':''}> Novidades</label><label><input type="checkbox" name="available" value="1" ${params.get('available')==='1'?'checked':''}> Em estoque</label></div><button class="btn btn-primary btn-wide">Aplicar filtros</button></form></aside><div><div class="catalog-toolbar"><div><button class="btn btn-secondary mobile-only" id="openFilters">${icon('filter')} Filtrar</button></div><label>Ordenar <select class="select" id="sortCatalog" style="width:auto"><option value="relevance">Relevância</option><option value="popular">Mais populares</option><option value="newest">Novidades</option><option value="price-asc">Menor preço</option><option value="price-desc">Maior preço</option><option value="rating">Avaliação</option><option value="discount">Maior desconto</option></select></label></div>${res.items.length?`<div class="product-grid">${res.items.map(productCard).join('')}</div>${pagination(res.pagination,params)}`:`<div class="empty-state">${icon('search')}<h2>Nenhum resultado</h2><p class="muted">Tente remover um filtro ou pesquisar outro termo.</p><a class="btn btn-primary" href="/catalogo" data-link>Limpar filtros</a></div>`}</div></div></section>`;}
function pagination(p,params){if(p.pages<=1)return'';return `<div class="pagination">${Array.from({length:p.pages},(_,i)=>{const q=new URLSearchParams(params);q.set('page',i+1);return `<a class="btn ${p.page===i+1?'btn-primary':''}" href="/catalogo?${q}" data-link>${i+1}</a>`}).join('')}</div>`;}
export function mountCatalog(){const form=$('#catalogFilters'),sort=$('#sortCatalog');if(!form)return;sort.value=new URLSearchParams(location.search).get('sort')||'relevance';form.onsubmit=e=>{e.preventDefault();const fd=new FormData(form),q=new URLSearchParams();for(const [k,v] of fd){if(String(v).trim())q.set(k,String(v).trim())}q.set('sort',sort.value);navigate(`/catalogo?${q}`)};sort.onchange=()=>{const q=new URLSearchParams(location.search);q.set('sort',sort.value);navigate(`${location.pathname}?${q}`)};const drawer=$('#filtersDrawer');$('#openFilters')?.addEventListener('click',()=>drawer.classList.add('open'));$('#closeFilters')?.addEventListener('click',()=>drawer.classList.remove('open'));hydrateIcons(document);}
